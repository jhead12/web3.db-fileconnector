// Route handlers for the {{plugin_name}} plugin
import express from "express";
import logger from "../../../logger/index.js";
import { getPluginContext } from "../../../utils/helpers.js";

const router = express.Router();

{
  {
    plugin_routes;
  }
}

export default router;
